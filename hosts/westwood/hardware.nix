{ config, lib, pkgs, ... }:

# ==============================================================================
# 硬件配置 —— 已拆分为子模块
#
#   本文件已被拆分为以下子模块，每个模块聚焦一个领域：
#
#   bootloader   → boot.nix（systemd-boot 引导）
#   NVIDIA GPU   → nvidia.nix（NVIDIA Optimus 混合显卡）
#   蓝牙         → bluetooth.nix（bluez 协议栈）
#   zram+swap    → swap.nix（交换空间策略）
#
#   ⚠️ 本文件保留仅为兼容旧引用，不会导入任何配置。
#      configuration.nix 已不再 import 本文件。
# ==============================================================================

{
  # 本文件为空，配置已分发到各子模块
}
