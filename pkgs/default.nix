# ==============================================================================
# 自定义软件包集合（Overlay 方式）
#   用于编写 nixpkgs 中没有的、本机专用的软件包。
#   本文件被 overlays/default.nix 引入。
# ==============================================================================
final: prev: {
  bilibili-tui = final.callPackage ./bilibili-tui { };
}
