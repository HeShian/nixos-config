# ==============================================================================
# 自定义辅助函数库
#
#   本文件定义了项目内共享的 Nix 辅助函数，用于：
#   - 封装重复出现的 Nix 表达式，减少各模块的冗余
#   - 提供便捷的简化接口，降低配置复杂度
#   - 集中管理跨模块公用的逻辑片段
#
#   使用方式（在任意 .nix 文件中）：
#     let
#       myLib = import ./lib { inherit lib; };
#     in {
#       # 使用 myLib.xxx 调用自定义函数
#     }
#
#   当前状态：函数库尚为空。随着配置复杂化，可在此添加工具函数，
#   例如 mkIfElse、range、merge 等实用组合子。
# ==============================================================================
{ lib, ... }: { }
